import './ringButton.css'
const RingButton = ({clickHandler}) => {
    //main button functionality
    return (
        <button id="clickButton" onClick={clickHandler} >
           
            </button>
    )
}

export default RingButton
